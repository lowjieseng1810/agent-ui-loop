# Acceptance contracts

Copy a YAML file, change `url` and selectors, run:

```bash
agent-ui-loop run --config contracts/login-responsive.yml --url http://localhost:3000
```

These files use only fields supported by the V3 schema (`docs/acceptance-contract.md`).
