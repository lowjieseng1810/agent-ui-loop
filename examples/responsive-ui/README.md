# Responsive UI

Same acceptance criteria on desktop and mobile. Use this as a starting contract for a marketing or app shell.

```bash
python examples/serve.py --fixed
agent-ui-loop run --config examples/responsive-ui/agent-ui-loop.yml --url http://127.0.0.1:48721
```
