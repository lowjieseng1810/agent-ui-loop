# Journey

Click Continue on desktop and assert the dashboard is visible. Journey primitives are `fill`, `click`, `visible`, `wait` only.

```bash
python examples/serve.py --fixed
agent-ui-loop run --config examples/journey/agent-ui-loop.yml --url http://127.0.0.1:48721
```
