# Security

Agent UI Loop is local-first. It opens the URL you configure in Chromium and writes evidence to disk. It does not upload screenshots or source by default.

Details: [docs/SECURITY.md](docs/SECURITY.md).

Until a dedicated security contact is published, report suspected vulnerabilities as a private maintainer message or a GitHub security advisory on the public repository, if one exists.
